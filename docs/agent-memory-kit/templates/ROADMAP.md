# Project roadmap

> The long-term goal tree for this project, shared by every human and AI agent working on this repository.
> [HANDOFF.md](HANDOFF.md) records the latest session and the current focus. This file records where the project is going.
> Protocol: [AGENTS.md](AGENTS.md). Commands: `python scripts/roadmap.py next | status | write | check | json`.

## How this file works

**Levels.** A goal (`G1`) is a lasting outcome. A sub-goal (`G1.1`) is a verifiable result inside it. A task (`G1.1.1`) is roughly one session of work.

**Horizon** is set on every sub-goal. Rewrite the meanings to fit this project, but keep the three tags:

| Tag | Meaning |
| --- | --- |
| `short-term` | Needed before real users rely on the project, or before the next release |
| `mid-term` | Expected in the months after that |
| `long-term` | Growth, scale and product decisions |

**Priority** within a horizon: `P0` critical, `P1` high, `P2` normal, `P3` low.

**Status:** `todo`, `doing`, `blocked` (add a `Blocked by:` line), `decision` (add a `Decision needed:` line; only the user decides), `done`, `dropped` (keep the entry and explain why).

**Sub-goal format.** The validator depends on it:

```
### G1.1 Short title
`short-term` `P0` `todo` needs: G2.1
Why: the problem, with evidence.
Evidence: how it was verified, and how strongly.
Done when: the observable result.
- [ ] G1.1.1 A concrete task.
- [x] G1.1.2 A finished task. (done 2026-01-01: the proof, such as a commit, test or file)
```

Optional lines: `From:` names an outside suggestion that was merged in. `Adapted from:` means the suggestion was changed to fit this project, with the reason.

**Rules for every agent**

1. Never renumber or reuse an ID. New entries take the next free number in their parent.
2. Never delete an entry. Set `dropped` and say why.
3. Check a task only with evidence in a `(done YYYY-MM-DD: ...)` note.
4. Update the sub-goal status in the same edit: `doing` when work starts, `done` when every task is checked and the "Done when" result is true.
5. Record newly discovered work here as tasks or sub-goals instead of leaving it in chat.
6. Never move a `decision` sub-goal forward without the user's answer.
7. Before finishing, run `scripts/roadmap.py write`, then `scripts/check_handoff.py --strict`, and log the session in `HANDOFF.md`.

## Summary

<!-- roadmap:summary:start -->
_Generated by `python scripts/roadmap.py write`. Do not edit this block by hand._

**Progress:** 1 of 6 tasks done. 1 of 5 sub-goals done.

**Sub-goal status:** todo 3, doing 0, blocked 0, decision 1, done 1, dropped 0.

| Goal | Short-term | Mid-term | Long-term | Tasks done |
| --- | --- | --- | --- | --- |
| G1. Testing and quality | 0/2 | - | 0/1 | 0/4 |
| G2. Operations | - | 0/1 | - | 0/1 |
| G3. Agent coordination | 1/1 | - | - | 1/1 |

Horizon cells count finished sub-goals out of all sub-goals in that horizon.

### Open short-term sub-goals

| ID | Sub-goal | Priority | Status | Needs |
| --- | --- | --- | --- | --- |
| G1.1 | Continuous integration | P0 | todo | G1.2 |
| G1.2 | Fast test tier | P1 | todo | - |

### Open mid-term sub-goals

| ID | Sub-goal | Priority | Status | Needs |
| --- | --- | --- | --- | --- |
| G2.1 | Hosting provider | P1 | decision | - |

### Open long-term sub-goals

| ID | Sub-goal | Priority | Status | Needs |
| --- | --- | --- | --- | --- |
| G1.3 | Load testing | P3 | todo | - |

### Decisions needed from the user

| ID | Decision needed | Horizon |
| --- | --- | --- |
| G2.1 | which hosting provider to use. | mid-term |

### Blocked

None.

<!-- roadmap:summary:end -->

## G1. Testing and quality

Outcome: every change is verified automatically. The goals below are examples; replace them with this project's real goal tree.

### G1.1 Continuous integration
`short-term` `P0` `todo` needs: G1.2
Why: nothing runs the tests automatically.
Evidence: example only; replace with what you verified and how.
Done when: every pull request runs the tests and both validators, and merging is blocked on failure.
- [ ] G1.1.1 Add a CI workflow that runs the test suite.
- [ ] G1.1.2 Run `python scripts/roadmap.py check` and `python scripts/check_handoff.py` in that workflow.

### G1.2 Fast test tier
`short-term` `P1` `todo`
Why: a slow suite discourages running tests between changes.
Done when: a documented subset of tests runs in a few minutes.
- [ ] G1.2.1 Mark slow tests and document the fast command in `AGENTS.md`.

### G1.3 Load testing
`long-term` `P3` `todo`
Why: behaviour under real traffic is unknown.
Done when: capacity and latency targets are measured and recorded.
- [ ] G1.3.1 Write load scenarios for the busiest paths and record the results.

## G2. Operations

Outcome: the project runs somewhere reliable and recoverable.

### G2.1 Hosting provider
`mid-term` `P1` `decision`
Why: deployment work cannot start until the platform is chosen.
Decision needed: which hosting provider to use.
Done when: the choice is recorded in `HANDOFF.md` section 7.
- [ ] G2.1.1 Compare two providers on cost and operational effort, then ask the user.

## G3. Agent coordination

Outcome: every session starts from shared memory and leaves it better.

### G3.1 Shared memory system
`short-term` `P0` `done`
Why: work and context were lost between sessions and tools.
Done when: `AGENTS.md`, `HANDOFF.md` and `ROADMAP.md` exist and both validators pass.
- [x] G3.1.1 Install the agent memory kit and fill in the project facts. (done 2026-01-01: python scripts/check_handoff.py --strict passed)

## Ideas not yet goals

Add ideas here as `- YYYY-MM-DD, author: idea`. Promote an idea to a sub-goal once someone commits to an outcome.

- None yet.

## Considered and not adopted

List suggestions that were rejected, each with the reason, so nobody proposes them again without a new argument. Record a change of mind in `HANDOFF.md` section 7.

- None yet.
