@AGENTS.md

Claude Code: the shared protocol and project facts live in `AGENTS.md` (imported above).
Read `HANDOFF.md` section 1 first, then run `python scripts/roadmap.py next`
to see open work and pending user decisions in `ROADMAP.md`.
Before you finish, update `ROADMAP.md` and `HANDOFF.md`, run `python scripts/roadmap.py write`,
then validate with `python scripts/check_handoff.py --strict`.
