import {useEffect,useState} from 'react';
import {useApp} from '../context';
import {api,setCsrf,download,readable,dateLabel} from '../api';
import type {SavedItem,User} from '../types';
import {clearOffline} from '../offline';
import {Button,Icon,Badge,Field,Notice,Toggle,Empty,Modal,Section,useAsync} from '../components/ui';
export default function Profile(){const {boot,setBoot,notify,navigate,refresh}=useApp();const [tab,setTab]=useState('preferences'),[p,setP]=useState(boot.user.preferences),[name,setName]=useState(boot.user.name),[modal,setModal]=useState<string|null>(null),[items,setItems]=useState<SavedItem[]>([]),[shares,setShares]=useState<{id:string;journeyId:string;expiresAt:number}[]>([]),[audit,setAudit]=useState<{action:string;at:number}[]>([]);const {busy,error,run}=useAsync();const update=(key:string,value:unknown)=>setP(s=>({...s,[key]:value}));
 const load=async()=>{if(['traveler','contact','favorite'].includes(tab))setItems(await api(`/records/${tab}`));if(tab==='privacy'){setShares(await api('/shares'));setAudit(await api('/audit'));}};useEffect(()=>{void run(load);},[tab]);
 const save=()=>run(async()=>{const user=await api<User>('/profile','PUT',{name,preferences:p});setBoot(b=>b?{...b,user}:b);notify('Profile and preferences saved.');});
 const logout=()=>run(async()=>{await api('/auth/logout','POST');await clearOffline();location.href='/';});
 const deleteModal=modal==='history'||modal==='account';
 return <><div className="page-heading"><div><span className="eyebrow">TRAVEL YOUR WAY</span><h1>Make Wayline yours</h1><p>Your preferences stay with your account.</p></div><div className="button-row">{boot.user.registered?<Button onClick={()=>void logout()}>Sign out</Button>:<><Button onClick={()=>setModal('login')}>Sign in</Button><Button kind="primary" onClick={()=>setModal('register')}>Create account</Button></>}</div></div><div className="profile-summary"><div className="large-avatar">{boot.user.name.split(' ').map(x=>x[0]).slice(0,2).join('')}</div><div><h2>{boot.user.name}</h2><p>{boot.user.email??'Guest session · create an account to return from another device'}</p></div><Badge tone="mint"><Icon name="lock" size={14}/> Private by default</Badge></div><div className="view-tabs scroll-tabs">{[['preferences','Preferences'],['accessibility','Accessibility'],['traveler','Travelers'],['contact','Contacts'],['favorite','Shortcuts'],['notifications','Notifications'],['privacy','Privacy'],['payments','Payments']].map(([k,v])=><button key={k} className={tab===k?'active':''} onClick={()=>setTab(k)}>{v}</button>)}</div>{error&&<Notice tone="error">{error}</Notice>}
 {tab==='preferences'&&<Section title="Your default trip preferences"><div className="form-grid"><Field label="Display name"><input value={name} maxLength={100} onChange={e=>setName(e.target.value)}/></Field><Field label="Routing priority"><select value={p.priority} onChange={e=>update('priority',e.target.value)}>{['balanced','price','fastest','reliable','walking','transfers','carbon'].map(x=><option key={x}>{x}</option>)}</select></Field><Field label="Default total budget ($)"><input type="number" min="0" max="10000" value={p.budgetCents/100} onChange={e=>update('budgetCents',Math.round(+e.target.value*100))}/></Field><Field label="Maximum walking (minutes)"><input type="number" min="0" max="120" value={p.maxWalkMinutes} onChange={e=>update('maxWalkMinutes',+e.target.value)}/></Field><Field label="Walking speed (meters / second)"><input type="number" min="0.3" max="2.5" step="0.1" value={p.walkSpeed} onChange={e=>update('walkSpeed',+e.target.value)}/></Field><Field label="Extra connection buffer (minutes)"><input type="number" min="0" max="120" value={p.minConnectionMinutes} onChange={e=>update('minConnectionMinutes',+e.target.value)}/></Field><Field label="Risk tolerance"><select value={p.riskTolerance} onChange={e=>update('riskTolerance',e.target.value)}>{['conservative','balanced','aggressive'].map(x=><option key={x}>{x}</option>)}</select></Field><Field label="Language for navigation & phrasebook"><select value={p.language} onChange={e=>update('language',e.target.value)}><option value="en">English</option><option value="es">Español</option><option value="hi">हिन्दी</option></select></Field></div><div className="toggle-grid">{[['lessCrowded','Prefer less crowded trips'],['preferTrain','Prefer trains'],['avoidBus','Avoid bus journeys'],['nightWalking','Reduce outdoor walking at night'],['coveredTransfers','Prefer shorter outdoor transfers'],['visitor','Visitor phrasebook']].map(([key,label])=><Toggle key={key} label={label} checked={Boolean(p[key as keyof typeof p])} onChange={v=>update(key,v)}/>)}</div><Button kind="primary" onClick={()=>void save()} disabled={busy}>Save preferences</Button></Section>}
 {tab==='accessibility'&&<Section title="Accessibility and travel assistance"><Notice>Routing uses step-free and walking preferences where supported. Assistance requests, service animals and announcement preferences must still be arranged with the operator.</Notice>{[['wheelchair','Wheelchair access'],['stepFree','Step-free paths'],['avoidStairs','Avoid stairs'],['elevatorRequired','Elevator required'],['lowFloor','Low-floor vehicle preferred'],['transferAssistance','Transfer assistance requested'],['serviceAnimal','Traveling with a service animal'],['visualAnnouncements','Visual announcements preferred'],['audioNavigation','Audio guidance preferred']].map(([key,label])=><Toggle key={key} label={label} description={['lowFloor','transferAssistance','serviceAnimal','visualAnnouncements','audioNavigation'].includes(key)?'Saved for trip preparation; operator confirmation required.':undefined} checked={Boolean(p[key as keyof typeof p])} onChange={v=>update(key,v)}/>)}<Button kind="primary" disabled={busy} onClick={()=>void save()}>Save accessibility preferences</Button></Section>}
 {['traveler','contact','favorite'].includes(tab)&&<Section title={tab==='traveler'?'Your travel companions':tab==='contact'?'Your trusted contacts':'Your saved shortcuts'} action={tab!=='favorite'?<Button icon="plus" onClick={()=>setModal(tab)}>Add {tab}</Button>:<Button onClick={()=>navigate('plan')}>Add from planner</Button>}>{tab==='contact'&&<Notice>Contacts are saved privately. Automatic SMS and email are not connected; reminders prompt you to contact someone yourself.</Notice>}{items.map(item=><div className="saved-item" key={item.id}><div><b>{item.name}</b><p>{item.contact??(item.fareClass?`${readable(item.fareClass)} · eligibility not verified`:`${item.from} → ${item.to}`)}</p></div><Button icon="trash" title={`Remove ${item.name}`} kind="icon-only" onClick={()=>void run(async()=>{await api(`/records/${tab}/${item.id}`,'DELETE');await load();})}/></div>)}{!items.length&&<Empty icon={tab==='contact'?'heart':'user'} title={`No ${tab==='favorite'?'shortcuts':tab+'s'} yet`}>Save only the details you want available for your trips.</Empty>}</Section>}
 {tab==='notifications'&&<Section title="A useful nudge, at the right time"><Toggle label="Critical connection and check-in alerts" checked={p.notifyCritical} onChange={v=>update('notifyCritical',v)}/><Toggle label="Routine travel updates" checked={p.notifyInfo} onChange={v=>update('notifyInfo',v)}/><div className="form-grid"><Field label="Quiet hours start · device time"><input type="time" value={p.quietStart} onChange={e=>update('quietStart',e.target.value)}/></Field><Field label="Quiet hours end · device time"><input type="time" value={p.quietEnd} onChange={e=>update('quietEnd',e.target.value)}/></Field><Field label="Arrival check-in delay (minutes)"><input type="number" min="5" max="1440" value={p.emergencyMinutes} onChange={e=>update('emergencyMinutes',+e.target.value)}/></Field><Field label="Preferred recovery spending limit ($)"><input type="number" min="0" max="1000" value={p.recoveryLimitCents/100} onChange={e=>update('recoveryLimitCents',Math.round(+e.target.value*100))}/></Field></div><Notice>Recovery limits are preferences only. Wayline cannot spend money or exchange tickets. Quiet hours apply to browser notifications; critical alerts can bypass quiet hours if enabled.</Notice><div className="button-row"><Button icon="bell" onClick={()=>void run(async()=>{if(!('Notification' in window))throw new Error('Notifications are unavailable in this browser.');const result=await Notification.requestPermission();notify(result==='granted'?'Browser notifications enabled while Wayline is open.':'Notification permission was not granted.');})}>Enable browser notifications</Button><Button kind="primary" disabled={busy} onClick={()=>void save()}>Save notification preferences</Button></div></Section>}
 {tab==='privacy'&&<div className="stack"><Section title="You control your travel history"><Toggle label="Save journey history" checked={p.saveHistory} onChange={v=>update('saveHistory',v)}/><Field label="Retention for new journeys"><select value={p.historyDays} onChange={e=>update('historyDays',+e.target.value)}><option value="0">Remove after trip</option><option value="7">7 days</option><option value="30">30 days</option><option value="90">90 days</option><option value="365">1 year</option></select></Field><div className="button-row wrap"><Button kind="primary" onClick={()=>void save()}>Save retention</Button><Button icon="download" onClick={()=>void run(async()=>download('wayline-personal-data.json',await api('/privacy/export')))}>Export my data</Button><Button icon="lock" onClick={()=>void run(async()=>{await clearOffline();notify('All offline packs on this browser were removed.');})}>Remove offline packs</Button></div></Section><Section title="Active sharing links">{shares.map(s=><div className="saved-item" key={s.id}><div><b>Journey share</b><p>Expires {new Date(s.expiresAt).toLocaleString()}</p></div><Button onClick={()=>void run(async()=>{await api(`/shares/${s.id}`,'DELETE');await load();notify('Sharing link revoked.');})}>Revoke</Button></div>)}{!shares.length&&<p>No active links. Your journeys are private.</p>}</Section><Section title="Recent account activity"><div className="audit-list">{audit.slice(0,10).map((a,i)=><div key={i}><span>{readable(a.action)}</span><small>{new Date(a.at).toLocaleString()}</small></div>)}</div></Section><Section title="Delete saved data"><p>Deletion removes app records and sharing links. It does not cancel carrier bookings.</p><div className="button-row"><Button kind="danger" onClick={()=>setModal('history')}>Delete travel history</Button><Button kind="danger" onClick={()=>setModal('account')}>Delete account</Button></div></Section></div>}
 {tab==='payments'&&<Section title="Book securely with your operator"><div className="payment-symbols"><Icon name="lock" size={42}/></div><h3>Your payment information stays with the carrier</h3><p>Unified checkout, payment methods, seat inventory and automatic rebooking require authorized carrier and payment integrations. No card information is collected here.</p><div className="provider-grid">{Object.entries(boot.operatorLinks).slice(0,6).map(([name,url])=><a className="provider-link" href={url} key={name} target="_blank" rel="noreferrer">{name}<Icon name="external" size={16}/></a>)}</div></Section>}
 {['register','login'].includes(modal??'')&&<Modal title={modal==='register'?'Create your Wayline account':'Welcome back'} onClose={()=>setModal(null)}><form className="stack" onSubmit={e=>{e.preventDefault();const d=Object.fromEntries(new FormData(e.currentTarget));void run(async()=>{const out=await api<{user:User;csrf:string}>(`/auth/${modal}`,'POST',d);setCsrf(out.csrf);await clearOffline();setBoot(prev=>prev?{...prev,...out}:prev);setP({...p,...out.user.preferences});setName(out.user.name);await refresh();setModal(null);notify('Signed in.');});}}>{modal==='register'&&<Field label="Your name"><input required name="name" maxLength={100} autoComplete="name" defaultValue={boot.user.name==='Traveler'?'':boot.user.name}/></Field>}<Field label="Email"><input name="email" required type="email" maxLength={254} autoComplete="email"/></Field><Field label="Password" hint={modal==='register'?'At least 12 characters.':undefined}><input name="password" type="password" required minLength={modal==='register'?12:1} maxLength={128} autoComplete={modal==='register'?'new-password':'current-password'}/></Field>{error&&<Notice tone="error">{error}</Notice>}<Button type="submit" kind="primary" disabled={busy}>{modal==='register'?'Create account':'Sign in'}</Button></form></Modal>}
 {['traveler','contact'].includes(modal??'')&&<Modal title={`Add a ${modal}`} onClose={()=>setModal(null)}><form className="stack" onSubmit={e=>{e.preventDefault();const f=new FormData(e.currentTarget),d=Object.fromEntries(f);void run(async()=>{await api(`/records/${modal}`,'POST',{...d,consent:f.has('consent'),assistance:f.has('assistance')});await load();setModal(null);notify('Saved.');});}}><Field label="Name"><input required name="name" maxLength={100}/></Field>{modal==='traveler'?<><Field label="Fare class"><select name="fareClass">{['adult','student','senior','military','accessibility','child'].map(x=><option key={x}>{x}</option>)}</select></Field><label className="check-row"><input type="checkbox" name="assistance"/>Assistance requested</label><Notice>Discount eligibility must be verified with the operator.</Notice></>:<><Field label="Contact information"><input required name="contact" maxLength={200} placeholder="Phone number or email"/></Field><label className="check-row"><input type="checkbox" name="consent"/>This person agrees to be my trip contact.</label></>}{error&&<Notice tone="error">{error}</Notice>}<Button type="submit" kind="primary" disabled={busy}>Save {modal}</Button></form></Modal>}
 {deleteModal&&<Modal title={modal==='account'?'Permanently delete your account?':'Delete travel history?'} onClose={()=>setModal(null)}><p>This cannot be undone. Type DELETE to confirm.</p><form className="stack" onSubmit={e=>{e.preventDefault();const confirm=new FormData(e.currentTarget).get('confirm');void run(async()=>{await api(`/privacy/${modal}`,'DELETE',{confirm});await clearOffline();if(modal==='account')location.href='/';else{setModal(null);await refresh();await load();notify('Travel history removed.');}});}}><Field label="Confirmation"><input required name="confirm" pattern="DELETE" autoComplete="off"/></Field>{error&&<Notice tone="error">{error}</Notice>}<Button type="submit" kind="danger" disabled={busy}>Permanently delete</Button></form></Modal>}
 </>;
}
